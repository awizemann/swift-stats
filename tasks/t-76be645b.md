---
id: t-76be645b
title: P7 Audit follow-ups: byte-offset marker, owned-dir guard, nextBatch loop, pump batching
status: done
added: 2026-08-19
priority: urgent
---

## Description

From P6 fresh-eye audit. EventStore: replace line-count queue.head with consumed BYTE OFFSET validated against newline boundary (fixes dup ingestion after interrupted marker write, torn-write mis-skip, removes per-append marker write); delete stale marker whenever consumedLines resets without successful write; only apply backup-exclusion/0700 to SDK-owned default dir, temp fallback gets own swift-stats/ subdir; de-recurse nextBatch into a loop. StatsClient: pumpRecorded appends all entries to pending and drains once. Tests for file-recreate, marker-write-failure relaunch, torn trailing line + append.

## Plan



## Artifacts

EventStore byte-offset marker, discardHead, torn-tail guard, nextBatch loop, ownsDirectory; StatsClient pump batching + temp subdir. 109+57 Swift tests green.

