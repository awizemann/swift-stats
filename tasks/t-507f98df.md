---
id: t-507f98df
title: P3 Client: cached seq, discard/drain race, record() API
status: done
added: 2026-08-19
priority: high
---

## Description

StatsClient.swift + StatsIdentityStore.swift: (a) cache seq in the actor; persist once per drain BEFORE records hit disk. (b) close the race where a performDrain already awaiting dispatcher.enqueue lands after discardAll() on setConsent/setEnabled(false). (c) add public nonisolated non-async record(_:props:) with ordered hand-off drained by the actor, capped buffer (~10k drop-newest, one log); track() remains. (d) replace preconditionFailure in identity with logged fallback. (e) README shows record(). Tests for ordering under burst, seq persistence, discard race. swift build && swift test pass. Self-audit.

## Plan



## Artifacts

Sources/Stats/StatsClient.swift (cached seq, discardGeneration, record()/drainRecorded(), logged identity fallback), README.md quick start, Tests/StatsTests/RecordTests.swift (8 tests). Open: 10k cap untested (cap not injectable); race fix asserted via interleavings.

